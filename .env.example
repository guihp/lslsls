# Required for Next.js (build + runtime). Set as Coolify env vars / build args.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role stays in Supabase Edge Function admin-create-user only — never in this app.
